<resources>
    <!-- Base application theme. -->
    <style name="Theme.BillTracker" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blueGrey</item>
        <item name="colorPrimaryVariant">@color/lightblue</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="colorAccent">@color/quantum_grey700</item>
        <item name="itemTextColor">@color/white</item>
        <item name="android:textColor">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">#424E5A</item>
        <item name="colorButtonNormal">@color/custom</item>
        <item name="android:textColorHighlight">@color/blue</item>
        <item name="android:textColorHint">@color/quantum_white_hint_text</item>
        <item name="colorSecondaryVariant">@color/custom</item>
        <item name="alertDialogTheme">@style/MyDialogStyle</item>
        <item name="colorError">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/black</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ButtonColor" parent="@android:style/Widget.Button">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>